# Variables de entorno para Docker Compose

# Frontend (raweb) - estas serán usadas como build args y environment en docker-compose.yml
API_URL=http://backend:5000
GEOSERVER_URL=http://localhost:8087/geoserver/wms # URL que el navegador del cliente usará para GeoServer

# Backend (raapi) - variables para la API Flask y la conexión a la base de datos
FLASK_ENV=development # o production
FLASK_APP=server.py # Ya está en el Dockerfile de la API, pero puede estar aquí por consistencia
FLASK_DEBUG=1 # Opcional, para modo debug de Flask si FLASK_ENV es development

# Base de Datos (PostgreSQL) - usadas por el servicio 'db' y 'backend'
DB_USER=raidis
DB_PASSWORD=raidis # Cambiar en un entorno real
DB_NAME=raidis
DB_HOST=db # El servicio backend se conectará a la BD usando este nombre de host
DB_PORT=5432 # Puerto interno de la BD en la red Docker. El servicio backend usa este.
            # El puerto expuesto al host de Windows es 5433, como se configuró.

# Nota: Los valores de DB_USER, DB_PASSWORD, DB_NAME también son usados por el servicio 'db'
# para la inicialización de PostgreSQL (variables POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB).
# docker-compose.yml ya usa la interpolación ${VARIABLE:-default} para esto.
# Mantenerlos consistentes es importante.
# Ejemplo: POSTGRES_USER=${DB_USER:-postgres} usará el valor de DB_USER de este archivo.
# Si DB_USER no está aquí, usará 'postgres'.
# Para asegurar que 'raidis' se use, definirlos aquí es bueno.
